substitutions:
  device_name: crow

external_components:
  - source: ../esphome/components
    components: [host_exec]

esphome:
  name: ${device_name}

host:
  mac_address: "b8:27:eb:4e:24:90"
host_exec:

api:
logger:

button:
  - platform: host_exec
    name: "Shutdown"
    command: "systemctl poweroff"
